{
    "mcpServers": {
        "radar": {
            "type": "stdio",
            "command": "npx",
            "args": ["-y", "--package", "@contextrail/radar@latest", "radar-mcp"],
            "envFile": "${workspaceFolder}/.env.radar"
        }
    }
}